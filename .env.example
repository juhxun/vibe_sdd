# 오늘의 할 일 앱 - 환경 설정 예시
# 이 파일을 .env로 복사하고 필요한 값을 설정해주세요.

# 데이터베이스 URL
# SQLite: sqlite:///./todos.db (상대 경로)
# SQLite: sqlite:////absolute/path/todos.db (절대 경로)
DATABASE_URL=sqlite:///./todos.db

# 서버 설정 (선택사항)
# HOST=127.0.0.1
# PORT=8000
# DEBUG=False
